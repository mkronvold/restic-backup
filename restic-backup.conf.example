# restic-backup.conf - Configuration file for restic-backup.sh
# Copy this file to restic-backup.conf and customize for your environment

# Repository location
# Local path example:
RESTIC_REPOSITORY="/backup/restic-repo"

# Remote repository examples:
# RESTIC_REPOSITORY="sftp:user@host:/backup/restic-repo"
# RESTIC_REPOSITORY="s3:s3.amazonaws.com/bucket-name"
# RESTIC_REPOSITORY="b2:bucketname:path/to/repo"

# Repository password
# Option 1: Password file (recommended - most secure)
# Store in ~/.secrets directory with restricted permissions
RESTIC_PASSWORD_FILE="$HOME/.secrets/restic-password"
# (Create: mkdir -p ~/.secrets && chmod 700 ~/.secrets)
# (Then: echo "your-password" > ~/.secrets/restic-password && chmod 600 ~/.secrets/restic-password)

# Option 2: Direct password (less secure - avoid for production)
# RESTIC_PASSWORD="change-me-to-a-secure-password"

# Backup targets (colon-separated list of directories)
BACKUP_TARGETS="/home/user/documents:/home/user/pictures:/home/user/projects:/etc"

# Optional: Additional restic options
# RESTIC_COMPRESSION="auto"
# RESTIC_CACHE_DIR="/var/cache/restic"

# Optional: Retention policy (for automatic pruning after backup)
# Uncomment and set values to enable automatic cleanup of old snapshots
# AUTO_PRUNE=true           # Set to false to disable automatic pruning (default: true)
# KEEP_LAST=7               # Keep last N snapshots
# KEEP_HOURLY=24            # Keep last N hourly snapshots
# KEEP_DAILY=7              # Keep last N daily snapshots
# KEEP_WEEKLY=4             # Keep last N weekly snapshots
# KEEP_MONTHLY=6            # Keep last N monthly snapshots
# KEEP_YEARLY=2             # Keep last N yearly snapshots
#
# Example: Keep last 7 snapshots, plus 1 daily for 14 days, 1 weekly for 8 weeks
# KEEP_LAST=7
# KEEP_DAILY=14
# KEEP_WEEKLY=8
# KEEP_MONTHLY=12
# KEEP_YEARLY=3
